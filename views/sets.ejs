<!DOCTYPE html>
<html lang="en" data-theme="pastel" class="bg-gray-200">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/main.css">
  <title>Sets - Lego Collection</title> 
</head>
<body>
  <%- include('partials/navbar', {page: '/lego/sets'}) %>
    
    <div class="hero bg-base-200 rounded-lg p-6">
      <div class="hero-content text-center">
        <div class="max-w-md">
          <h1 class="text-5xl font-bold">Collection</h1>

          <p class="py-6">
            Browse the entire collection. Click the theme to filter by themes
            such as:
            <a href="/lego/sets?theme=Technic" class="btn btn-primary btn-xs">Technic</a>
            <a href="/lego/sets?theme=Classic Town" class="btn btn-primary btn-xs">Classic Town</a>
            <a href="/lego/sets?theme=Supplemental" class="btn btn-primary btn-xs">Supplemental</a>
          </p>
        </div>
      </div>
    </div>
          <div>
                <table class="table">
                  <tbody>
                    <!-- rows -->
                    <% legoSets.forEach(set => { %>
                    <tr class="even:bg-transparent odd:bg-zinc-50 rounded-lg">
                      <td>
                        <div class="flex items-center gap-3">
                          <div class="avatar">
                            <div class="mask mask-squircle w-12 h-12">
                              <img src="<%= set.img_url %>" alt="Avatar"/>
                            </div>
                          </div>
                        </div>
                      </td>
                      <td>
                        <div class="font-bold"><%= set.name %>  |  <span><a class="btn btn-ghost btn-xs" href="/lego/sets?theme=<%= set.Theme.name %>"><%= set.Theme.name %></a></td>
                        </span></div> 
                       
                        
                      </td>
                      <td><strong>Year:</strong><%= set.year %></td>
                      <td> <strong>Parts: </strong><%= set.num_parts %></td>
                      <td>
                        <a class="btn btn-ghost btn-xs" href="/lego/sets/<%= set.set_num %>">DETAILS</a></td>
                      </td>
                    </tr>
                    <% }); %>
                  </tbody>
                </table>
              </div>
              
          </div>
</body>
</html>